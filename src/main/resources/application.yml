discord:
  token: ${DISCORD_BOT_TOKEN:}  # Set via environment variable or .env file
  startup-notification:
    enabled: true
    message: "🚀 **Crazy Counter Bot is online!** Ready to start counting. Type `!counter status` for help."

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true

logging:
  level:
    com.example.crazycounter: DEBUG
    net.dv8tion.jda: INFO

---
# Docker Profile - MySQL Configuration
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://mysql:3306/crazy_counter?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}
    username: ${SPRING_DATASOURCE_USERNAME:counter_user}
    password: ${SPRING_DATASOURCE_PASSWORD:counter_password}
  jpa:
    database-platform: ${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQLDialect}
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    show-sql: ${SPRING_JPA_SHOW_SQL:true}
  h2:
    console:
      enabled: false

logging:
  level:
    com.example.crazycounter: ${LOGGING_LEVEL_COM_EXAMPLE_CRAZYCOUNTER:INFO}
    net.dv8tion.jda: INFO